import type { ModuleCatalog } from '../messages';

// Datasets module strings (dataset detail, data model canvas, measures panel,
// manage columns, calculated/query tables, relationships, quality panel).
export const datasetsCatalog: ModuleCatalog = {
  en: {},
  vi: {},
};
