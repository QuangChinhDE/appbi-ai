import type { ModuleCatalog } from '../messages';

// Explore module strings (editor, chart config, source selector, filter builder,
// advanced charts).
export const exploreCatalog: ModuleCatalog = {
  en: {},
  vi: {},
};
