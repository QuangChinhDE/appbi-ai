import type { ModuleCatalog } from '../messages';

// Workboards module strings (builder chrome, canvas overview, screen editors,
// app settings, RLS, users, webhooks, import/export, publish toggle, share).
export const workboardsCatalog: ModuleCatalog = {
  en: {},
  vi: {},
};
