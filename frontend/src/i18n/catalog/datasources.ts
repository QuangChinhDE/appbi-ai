import type { ModuleCatalog } from '../messages';

// Data Sources module strings (connection form, source list page, permissions).
export const datasourcesCatalog: ModuleCatalog = {
  en: {},
  vi: {},
};
