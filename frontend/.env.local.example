# Environment
# Keep API URL relative so the same frontend works for localhost and custom domains.
NEXT_PUBLIC_API_URL=/api/v1
# AI service URLs are derived at runtime from window.location.
# Only set explicit overrides if the AI services live on a different host.
# NEXT_PUBLIC_AI_WS_URL=ws://localhost:8001/chat/ws
