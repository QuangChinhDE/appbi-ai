# <Feature> — intent

**Status:** draft · in progress · shipped
**Owner:**
**Date:**

## Problem

What is wrong or missing today, from the point of view of someone using AppBI. Describe
the current behaviour concretely — a screen, a number, a step that fails — not the absence
of the feature.

## Goal

What is true after this ships, in one or two sentences.

## Out of scope

What this change deliberately does **not** do. This section prevents scope drift later and
is the most useful part of the file.

## Constraints

Anything that limits the solution space: existing data shapes, permission model, protected
subsystems involved, performance budget, backwards compatibility, an existing contract that
must not change.

## Acceptance criteria

Numbered, checkable statements. Each one should be something a reviewer can verify against
the running system or a test — not "works well" but "a viewer with `reports: view` sees X
and cannot reach Y".

1.
2.
3.

## Open questions

Anything that needs an answer before or during implementation, and who can answer it.
